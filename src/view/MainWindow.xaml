<Window
    x:Class="Logavise.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:local="clr-namespace:Logavise"
    Title="Logavise"
    Width="800"
    Height="450"
    MinWidth="380"
    MinHeight="261">
    <Window.Resources>
        <local:BooleanToBrushConverter x:Key="saveFileIconConverter" />
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewFileCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenFileCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveFileCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl+Alt" Command="{Binding SaveFileAsCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" Command="{Binding SaveAllFilesCommand}" />
        <KeyBinding Key="W" Modifiers="Ctrl" Command="{Binding CloseFileCommand}" />
        <KeyBinding Key="W" Modifiers="Ctrl+Shift" Command="{Binding CloseAllFilesCommand}" />
    </Window.InputBindings>

    <DockPanel Background="GhostWhite">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Command="{Binding NewFileCommand}" Header="_New">
                    <MenuItem.InputGestureText>Ctrl+N</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding OpenFileCommand}" Header="_Open…">
                    <MenuItem.InputGestureText>Ctrl+O</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding SaveFileCommand}" Header="Save">
                    <MenuItem.InputGestureText>Ctrl+S</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding SaveFileAsCommand}" Header="Save As…">
                    <MenuItem.InputGestureText>Ctrl+Alt+S</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding SaveAllFilesCommand}" Header="Save All">
                    <MenuItem.InputGestureText>Ctrl+Shift+S</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding CloseFileCommand}" Header="Close">
                    <MenuItem.InputGestureText>Ctrl+W</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Command="{Binding CloseAllFilesCommand}" Header="Close All">
                    <MenuItem.InputGestureText>Ctrl+Shift+W</MenuItem.InputGestureText>
                </MenuItem>
                <MenuItem Header="Recent Files" />
                <Separator />
                <MenuItem Command="{Binding ExitCommand}" Header="Exit">
                    <MenuItem.InputGestureText>Alt+F4</MenuItem.InputGestureText>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="Cut" />
                <MenuItem Header="Copy" />
                <MenuItem Header="Paste" />
                <MenuItem Header="Duplicate" />
                <MenuItem Header="Delete" />
                <MenuItem Header="Select All" />
            </MenuItem>
            <MenuItem Header="Search">
                <MenuItem Header="Go To" />
                <MenuItem Header="Find" />
            </MenuItem>
            <MenuItem Header="View" />
            <MenuItem Header="Config" />
            <MenuItem Header="Settings" />
            <MenuItem Header="Window" />
            <MenuItem Header="_Help">
                <MenuItem Header="View Help" />
                <MenuItem Header="About Logavise" />
            </MenuItem>
        </Menu>

        <Border Padding="8,8,8,8" DockPanel.Dock="Bottom">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border
                    Grid.Row="0"
                    Padding="1,1,1,1"
                    Background="DarkGray">
                    <avalon:TextEditor
                        x:Name="consoleEditor"
                        Height="98"
                        HorizontalScrollBarVisibility="Hidden"
                        PreviewKeyDown="consoleEditor_PreviewKeyDown"
                        ShowLineNumbers="True" />
                </Border>

                <Grid
                    Grid.Row="1"
                    Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="60" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Grid.Column="0"
                        Padding="0,0,0,0"
                        Background="DarkGray">
                        <TextBox
                            Grid.Column="0"
                            x:Name="consoleLine"
                            KeyDown="consoleLine_KeyDown"/>
                    </Border>

                    <Button Grid.Column="1" Margin="2,0,0,0" Height="20" Width="Auto" >
                        <Image Source="/src/img/arrow-right.png" />
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <Border Padding="8,8,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TabControl
                    x:Name="tabControl"
                    Grid.Row="0"
                    ItemsSource="{Binding EditorTabs}"
                    SelectionChanged="tabControl_SelectionChanged">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Height="Auto" Orientation="Horizontal">
                                <Border Height="14" Margin="0,-5,4,0" Background="{Binding IsModified, Converter={StaticResource saveFileIconConverter}, ConverterParameter='Red|CornflowerBlue'}">
                                    <Image Height="14" Width="14" Source="/src/img/disk.png" />
                                </Border>
                                <TextBlock
                                    Height="24"
                                    Margin="0,0,4,0"
                                    Text="{Binding Header}" />
                                <Button
                                    Width="14"
                                    Height="14"
                                    Margin="0,-5,0,0"
                                    Click="CloseTabButton_Click"
                                    Focusable="False"
                                    Tag="{Binding Index}">
                                    <Image Source="/src/img/cross-small.png" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <!--  Empty template  -->
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>
                <Border
                    Grid.Row="1"
                    Padding="1,0,1,1"
                    Background="DarkGray">
                    <avalon:TextEditor
                        x:Name="textEditor"
                        Grid.Row="1"
                        FontFamily="Consolas"
                        HorizontalScrollBarVisibility="Auto"
                        ShowLineNumbers="True"
                        TextChanged="textEditor_TextChanged"
                        />
                </Border>
            </Grid>

        </Border>
    </DockPanel>
</Window>
